interface:
  display_name: "Ban Create Issue"
  short_description: "Create provider issues and branches"
  default_prompt: "Use $ban-create-issue to create a provider-tracked issue or work item, then create a linked local branch."
